<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include("./partials/css.ejs") %> 
    <title>Document</title>
  </head>
  <body>

    <%- include("./partials/navbar.ejs") %>

    <% if (success) { %>
    <div class="card-panel teal lighten-2"><%= success %></div>
    <% } %>
    <div class="container">
    <% if (many) { %>
      <form class="col s6" action="/files/upload-many" method="post" enctype="multipart/form-data">
      <% } else { %>
        <form action="/files/upload"  class="col s6"method="post" enctype="multipart/form-data">
      <% } %>
      <div class="file-field input-field">
        <div class="btn">
          <span>File</span>
          <% if (many) { %>
          <input type="file" name="myImages" multiple />
          <% } else { %>
          <input type="file" name="myImage" />
          <% } %>
        </div>
        <div class="file-path-wrapper">
          <input
            class="file-path validate"
            type="text"
            placeholder="Select File"
          />
        </div>
      </div>
      <button class="btn waves-effect waves-light" type="submit" name="action">
        Submit
      </button>
    </form>
  </div>
    <%- include("./partials/js.ejs") %> 
  </body>
</html>
